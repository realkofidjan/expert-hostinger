{
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build:frontend": "cd frontend && npm install --include=dev && npm run build",
    "build": "npm run deploy:prep",
    "deploy:prep": "npm run build:frontend && rm -rf backend/public && mkdir -p backend/public && cp -r frontend/dist/* backend/public/",
    "start": "cd backend && node index.js",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "typescript": "^6.0.2"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
